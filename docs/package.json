{
  "name": "lidimist-manifesto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bundle exec jekyll serve",
    "live-dev": "bundle exec jekyll serve --livereload"
	"build": "./build.sh"
	"deploy": "build && ./deploy.sh"
  },
  "devDependencies": {},
  "dependencies": {}
}
